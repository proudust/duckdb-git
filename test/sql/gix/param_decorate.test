# name: test/sql/gix/param_decorate.test
# description: decorate column and decorate parameter (gix backend)
# group: [git]

require duckdb_git

require-env GIX_BACKEND 1

# Test that decorate is an empty array for a commit with no branch/tag pointing to it
query I
SELECT decorate FROM git_log('.', revision='2e6d5e79dafd8ff8c09152ac35e32cd26e65efe5', max_count=1, backend='gix');
----
[]

# Test that decorate includes an annotated tag pointing to the commit (exercises tag peeling)
query I
SELECT decorate FROM git_log('.', revision='295db8704f2b2e12fe71a1f433b8b17906fedf25', max_count=1, backend='gix');
----
[v0.1.1]

# decorate='full' returns full ref paths
query I
SELECT decorate FROM git_log('.', revision='295db8704f2b2e12fe71a1f433b8b17906fedf25', max_count=1, decorate='full', backend='gix');
----
[refs/tags/v0.1.1]

# decorate='no' is rejected (use projection pushdown to omit the column)
statement error
SELECT decorate FROM git_log('.', decorate='no', backend='gix');
----
decorate='no' is not supported; omit the decorate column instead
